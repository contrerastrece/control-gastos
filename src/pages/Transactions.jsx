import React from 'react'
import styled from 'styled-components'
import { TransactionTemplate } from '../index';

export const Transactions = () => {
  return (
    <Container>
      <TransactionTemplate/>
    </Container>
  )
}
const Container=styled.div`

`;